<div class="books">
  
  <div class="success-message">
    <%= flash[:notice]  %>
  </div>
  
  <h1>Books</h1>
  
  <table>
    <thead>
      <tr>
        <th>Title</th>
        <th>Body</th>
        <th></th>
      </tr>
    </thead>
    
    <tbody>
      <% @books.each do |book| %>
      <tr>
       <td><%= book.title %></td>
       <td><%= book.body %></td>
       <td>
         <%= link_to "Show",book_path(book) %>
       </td>
       <td>
         <%= link_to "Edit",edit_book_path(book.id) %>
       </td>
       <td>
         <%= link_to "destroy",book_path(book.id),method: :delete,"data-confirm" => "削除しますか？"%>
       </td>
      </tr>
      <% end %>
    </tbody>
    
  </table>
  
  <div class="new-create">
    <div class="new-title">
     <h2>New book</h2>
    </div>
    <div class="error-message">
    <% if @book.errors.any? %>
      <%= @book.errors.count %> error has occurred
      <ul>
        <% @book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    <% end %>
    </div>
    
    <%= form_with model: @book,url: "/books", method: :post do |f| %>
    <div class="books-text">
      <div class="title-box">
        <p>Title</p>
        <%= f.text_field:title %>
      </div>
      <div class="body-box">
        <p>Body</p>
        <%= f.text_area:body %>
      </div>
      <div class="btn-box">
        <%= f.submit "Create Book" %>
      </div>
    </div>
    <% end %>
    
  </div>
</div>